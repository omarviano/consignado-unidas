import React from 'react';

import { ContractCardProps } from './props';
import * as Styled from './styles';

const ContractCard: React.FC<ContractCardProps> = ({ data }) => (
  <Styled.Container>a</Styled.Container>
);

export { ContractCard };
