import styled from 'styled-components';

import FormControlStyles from '@material-ui/core/FormControl';
import TextFieldStyles from '@material-ui/core/TextField';

export const InputOutlined = styled(TextFieldStyles)`
  .MuiOutlinedInput-root {
    border-radius: 8px;
    height: 38px;
  }
`;

export const FormControl = styled(FormControlStyles)``;
