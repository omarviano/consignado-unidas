import styled from 'styled-components';
import TooltipStyles from '@mui/material/Tooltip';

export const Tooltip = styled(TooltipStyles)``;

export const TooltipText = styled.span`
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
`;
